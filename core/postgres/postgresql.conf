# PostgreSQL configuration for development environment
# Prioritizes reduced disk usage over replication/backup features

# Minimal WAL logging - only what's needed for crash recovery
wal_level = minimal
max_wal_senders = 0

# Disable archive mode (not needed in development)
archive_mode = off
